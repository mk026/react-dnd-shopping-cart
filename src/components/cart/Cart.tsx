import { FC } from "react";

import CartControls from "./cart-controls";

const Cart: FC = () => {
  return (
    <div>
      <p>Cart</p>
      <CartControls />
    </div>
  );
};

export default Cart;
